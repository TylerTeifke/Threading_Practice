#ifndef MULTITHREAD_H
#define MULTITHREAD_H
#include <string>
#include <vector>
#include <thread>
#include <iostream>
#include <chrono>
using namespace std;
#pragma once

void threaded_solution(int thread_num, vector<string> words);

#endif